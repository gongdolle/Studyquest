{
  "type": "object",
  "properties": {
    "title": { "type": "string" },
    "strategy": { "type": "string" },
    "skillNodes": {
      "type": "array",
      "minItems": 1,
      "maxItems": 80,
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "prerequisites": { "type": "array", "items": { "type": "string" } },
          "reason": { "type": "string" }
        },
        "required": ["name", "prerequisites", "reason"],
        "additionalProperties": false
      }
    },
    "days": {
      "type": "array",
      "minItems": 7,
      "maxItems": 7,
      "items": {
        "type": "object",
        "properties": {
          "day": { "type": "integer", "minimum": 1, "maximum": 7 },
          "focus": { "type": "string" },
          "quests": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "properties": {
                "title": { "type": "string" },
                "durationMin": { "type": "integer", "minimum": 10, "maximum": 90 },
                "output": { "type": "string" },
                "skillNames": {
                  "type": "array",
                  "minItems": 1,
                  "maxItems": 5,
                  "items": { "type": "string" }
                }
              },
              "required": ["title", "durationMin", "output", "skillNames"],
              "additionalProperties": false
            }
          }
        },
        "required": ["day", "focus", "quests"],
        "additionalProperties": false
      }
    }
  },
  "required": ["title", "strategy", "skillNodes", "days"],
  "additionalProperties": false
}
