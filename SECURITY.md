# Security Policy

StudyQuest는 AI 공급자 인증, 로컬 학습 데이터, 문서 본문, 마이크 권한과 AI 생성 JavaScript를 다루므로 보안 문제를 공개 이슈로 보고하지 마세요.

## 지원 범위

초기 개발 단계에서는 최신 GitHub Release와 기본 브랜치의 최신 커밋만 최선의 노력으로 지원합니다. 이전 빌드에서 발견한 문제는 최신 빌드에서도 재현되는지 먼저 확인해 주세요.

## 비공개 보고 방법

다음 GitHub 비공개 채널만 사용해 주세요.

1. 저장소의 `Security` 탭을 엽니다.
2. Private vulnerability reporting이 활성화되어 있다면 `Report a vulnerability`를 선택합니다.
3. 저장소 관리자 또는 협업자로서 조정 중이라면 `Security` → `Advisories`에서 비공개 draft security advisory를 사용합니다.

공개 Issue, Discussion, Pull Request에 취약점 세부 정보, 실제 API 키, 인증 토큰, 개인 문서 또는 재현 가능한 공격 코드를 올리지 마세요.

## 보고서에 포함할 내용

- 영향을 받는 StudyQuest 버전 또는 커밋
- Windows 버전과 실행 방식(Release ZIP 또는 소스 실행)
- 영향을 받는 구성요소와 예상 영향
- 재현에 필요한 최소 단계
- 실제 비밀 값과 개인 데이터를 제거한 로그 또는 화면
- 가능한 경우 안전한 수정 방향

재현에는 폐기 가능한 테스트 계정과 가짜 데이터를 사용해 주세요. 다른 사용자의 시스템이나 공급자 계정을 허가 없이 시험하지 마세요.

## 특히 중요한 보고 대상

- API 키나 CLI 인증 정보가 평문 또는 학습 상태에 저장되는 문제
- renderer 또는 생성 위젯에서 Node.js·preload bridge·부모 앱에 접근하는 문제
- iframe에서 네트워크, 파일, 팝업 또는 허용되지 않은 탐색이 가능한 문제
- 임의 IPC 호출, 경로 탈출 또는 Windows 시스템 디렉터리 쓰기
- 문서나 음성이 사용자 동작 없이 외부로 전송되는 문제
- AI 공급자 프로세스의 의도한 읽기 전용·도구 비활성 경계를 우회하는 문제

## 처리와 공개

정해진 응답 시간 SLA는 없지만, 관리자는 가능한 범위에서 보고를 확인하고 영향과 수정 방향을 비공개 advisory에서 조정합니다. 수정 또는 완화책을 제공하기 전에 상세 내용을 공개하지 말아 주세요. 공개 시점과 표현은 보고자와 관리자가 advisory 안에서 협의합니다.

실제 API 키나 인증 정보가 노출되었다면 StudyQuest의 답변을 기다리지 말고 해당 공급자에서 즉시 폐기하고 새 키를 발급하세요.
